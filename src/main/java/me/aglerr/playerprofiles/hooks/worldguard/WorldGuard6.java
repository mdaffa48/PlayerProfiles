package me.aglerr.playerprofiles.hooks.worldguard;

public class WorldGuard6{

    /*public static List<String> getRegions(@NotNull Location location) {
        // Get the ApplicableRegionSet from the location
        com.sk89q.worldguard.protection.ApplicableRegionSet ars = com.sk89q.worldguard.bukkit.WGBukkit.getPlugin().getRegionContainer().createQuery().getApplicableRegions(location);
        // Create an empty array list of string
        List<String> regions = new ArrayList<>();
        // Loop through all regions in the location
        for(com.sk89q.worldguard.protection.regions.ProtectedRegion region : ars){
            // Add the region name to the list
            regions.add(region.getId());
        }
        // Finally, return the list of regions name
        return regions;
    }*/

}
